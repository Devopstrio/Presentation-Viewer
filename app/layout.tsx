import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Devopstrio Standup Call - PPT Presentation Viewer",
  description: "Upload and present your slides in fullscreen mode with smooth transitions and auto-play timers.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} h-full antialiased`}
    >
      <body className="min-h-full bg-background text-white-text flex flex-col">
        {children}
      </body>
    </html>
  );
}
